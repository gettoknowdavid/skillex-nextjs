import { styled, Theme } from 'baseui';

export const HeroPersonsDiv = styled('div', ({ $theme }) => ({
  opacity: '1',
  position: 'relative',
  zIndex: 0,
  display: 'flex',
  height: '590px',
  marginRight: '-20px',
  marginLeft: '-85px',
  justifyContent: 'flex-end',
}));
