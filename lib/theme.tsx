import { createLightTheme, lightThemePrimitives } from 'baseui';

const SkillexTheme = createLightTheme({
  ...lightThemePrimitives,
  primaryA: '#1e1e2f',
  primaryFontFamily: '"Archia", sans-serif',
}, {

});

export default SkillexTheme;
